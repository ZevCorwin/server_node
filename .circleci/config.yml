version: 2.1

jobs:
  build:
    docker:
      - image: circleci/node:16-browsers
    steps:
      - checkout

      - run:
          name: Install Dependencies
          command: npm install

      - run:
          name: Run Tests
          command: echo "No tests available"

      - run:
          name: Start Server
          command: echo "Server can be started locally for testing"
